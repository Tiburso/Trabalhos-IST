#include <stdio.h>

void piramide(int N) {
  int i = 0;
  for (size_t _ = 1; _ <= N; _++) {
    while i
    printf("%s\n", );
  }
}

int main(int argc, char const *argv[]) {
  int N;
  do {
    printf("Escreva um tamanho:");
    scanf("%d", &N);
  } while(N < 2);
  piramide(N);
  return 0;
}
